#include<bits/stdc++.h>
#include <iostream>
#include <string>
//#include <vector>
//#include <math.h>

#define LOG(x)          cout << (x) << endl;
//#define ARR_LOG(x)      for(auto c : x) {LOG(c)};
//#define ALL(x)      (x).begin(), (x).end()

using namespace std;


int main ()
{
    
    unsigned int inpNum = 0;
    cin >> inpNum;
    
    /*
    unsigned long long res = 5;
    
    while (inpNum != 1)
    {
        res *= 5;
        inpNum--;
    }
    
    */
    LOG(26)
    //LOG(res % 100)
    
    return 0;
}