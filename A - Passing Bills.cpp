#include <iostream>
//#include <math.h>
//#include <string>
//#include <vector>
 
using namespace std;
#define LOG(x)      cout << (x) << endl;
//#define ALL(x)      (x).begin(), (x).end()
 
 
 
int main ()
{
    int n = 0;
    int g = 0;
    
    cin >> n >> g;
    LOG(n)
    
    
    return 0;
}